name: Sync Dev Branches on PR Merge

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-dev-branches:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

      - name: Sync dev branches
        run: |
          git fetch origin
          
          DEV_BRANCHES=$(git branch -r | grep 'origin/dev/' | grep -v HEAD | sed 's/origin\///' | xargs)
          
          if [ -z "$DEV_BRANCHES" ]; then
            echo "ℹ️ Aucune branche dev/* à synchroniser"
            exit 0
          fi
          
          echo "🔄 Branches à synchroniser: $DEV_BRANCHES"
          
          SYNC_SUCCESS=0
          SYNC_SKIPPED=0
          SYNC_FAILED=0
          
          for branch in $DEV_BRANCHES; do
            echo ""
            echo "📝 Vérification de $branch..."
            
            # Checkout de la branche
            if ! git checkout $branch; then
              echo "❌ Impossible de checkout $branch"
              ((SYNC_FAILED++))
              continue
            fi
            
            # Vérifier si la branche est déjà à jour
            git fetch origin $branch
            LOCAL_COMMIT=$(git rev-parse HEAD)
            MAIN_COMMIT=$(git rev-parse origin/main)
            
            # Vérifier si main a des commits que la branche n'a pas
            BEHIND_COUNT=$(git rev-list --count HEAD..origin/main)
            
            if [ "$BEHIND_COUNT" -eq 0 ]; then
              echo "✅ $branch déjà à jour (aucun commit à synchroniser)"
              ((SYNC_SKIPPED++))
              continue
            fi
            
            echo "🔄 $branch en retard de $BEHIND_COUNT commits, synchronisation..."
            
            # Tentative de merge
            if git merge origin/main --no-edit; then
              # Vérifier s'il y a quelque chose à pousser
              if git diff --quiet HEAD origin/$branch; then
                echo "✅ $branch synchronisée localement (rien à pousser)"
                ((SYNC_SUCCESS++))
              else
                # Il y a des changements, on pousse
                if git push origin $branch; then
                  echo "✅ $branch synchronisée et poussée"
                  ((SYNC_SUCCESS++))
                else
                  echo "❌ Échec du push pour $branch"
                  ((SYNC_FAILED++))
                fi
              fi
            else
              echo "⚠️ Conflits détectés sur $branch - abandonnée"
              git merge --abort
              ((SYNC_FAILED++))
            fi
          done
          
          # Retour sur main
          git checkout main
          
          echo ""
          echo "📊 Résumé:"
          echo "   ✅ Synchronisées: $SYNC_SUCCESS"
          echo "   ⏭️  Déjà à jour: $SYNC_SKIPPED" 
          echo "   ❌ Échecs: $SYNC_FAILED"
          
          # L'action n'échoue que si toutes les branches ont échoué
          if [ $SYNC_FAILED -gt 0 ] && [ $((SYNC_SUCCESS + SYNC_SKIPPED)) -eq 0 ]; then
            echo "::error::Toutes les synchronisations ont échoué"
            exit 1
          elif [ $SYNC_FAILED -gt 0 ]; then
            echo "::warning::Certaines branches n'ont pas pu être synchronisées"
          fi
