name: Sync Dev Branches on PR Merge

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-dev-branches:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

      - name: Sync dev branches
        run: |
          # Pas de set -e global, on gère les erreurs manuellement
          set +e
          
          git fetch origin
          
          DEV_BRANCHES=$(git branch -r | grep 'origin/dev/' | grep -v HEAD | sed 's/origin\///' | xargs)
          
          if [ -z "$DEV_BRANCHES" ]; then
            echo "ℹ️ Aucune branche dev/* à synchroniser"
            exit 0
          fi
          
          echo "🔄 Branches à synchroniser: $DEV_BRANCHES"
          
          SYNC_SUCCESS=0
          SYNC_SKIPPED=0
          SYNC_FAILED=0
          
          for branch in $DEV_BRANCHES; do
            echo ""
            echo "📝 Vérification de $branch..."
            
            # Checkout de la branche
            if git checkout $branch; then
              echo "✅ Checkout $branch réussi"
            else
              echo "❌ Impossible de checkout $branch"
              ((SYNC_FAILED++))
              continue
            fi
            
            # Fetch de la branche spécifique
            git fetch origin $branch
            
            # Vérifier si la branche est déjà à jour
            BEHIND_COUNT=$(git rev-list --count HEAD..origin/main 2>/dev/null || echo "0")
            
            if [ "$BEHIND_COUNT" -eq 0 ]; then
              echo "✅ $branch déjà à jour (aucun commit à synchroniser)"
              ((SYNC_SKIPPED++))
              continue
            fi
            
            echo "🔄 $branch en retard de $BEHIND_COUNT commits, synchronisation..."
            
            # Tentative de merge
            if git merge origin/main --no-edit; then
              echo "✅ Merge réussi pour $branch"
              
              # Push
              if git push origin $branch; then
                echo "✅ $branch synchronisée et poussée"
                ((SYNC_SUCCESS++))
              else
                echo "❌ Échec du push pour $branch"
                ((SYNC_FAILED++))
              fi
            else
              echo "⚠️ Conflits détectés sur $branch - abandon du merge"
              git merge --abort 2>/dev/null || true
              ((SYNC_FAILED++))
            fi
          done
          
          # Retour sur main
          echo ""
          echo "🏠 Retour sur la branche main..."
          git checkout main
          
          echo ""
          echo "📊 Résumé final:"
          echo "   ✅ Synchronisées: $SYNC_SUCCESS"
          echo "   ⏭️  Déjà à jour: $SYNC_SKIPPED" 
          echo "   ❌ Échecs: $SYNC_FAILED"
          
          # L'action réussit si au moins une branche a été traitée avec succès
          TOTAL_SUCCESS=$((SYNC_SUCCESS + SYNC_SKIPPED))
          
          if [ $TOTAL_SUCCESS -gt 0 ]; then
            echo "🎉 Synchronisation globale réussie"
            exit 0
          elif [ $SYNC_FAILED -gt 0 ]; then
            echo "💥 Toutes les synchronisations ont échoué"
            exit 1
          else
            echo "🤷 Aucune branche traitée"
            exit 0
          fi
